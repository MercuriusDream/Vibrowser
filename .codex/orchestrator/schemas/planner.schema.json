{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "workloads": {
      "type": "array",
      "minItems": 6,
      "maxItems": 6,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "title": { "type": "string" },
          "objective": { "type": "string" },
          "owned_paths": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" }
          },
          "verification": { "type": "string" },
          "prompt_seed": { "type": "string" }
        },
        "required": ["title", "objective", "owned_paths", "verification", "prompt_seed"]
      }
    }
  },
  "required": ["workloads"]
}
